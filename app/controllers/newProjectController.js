app.controller("newProjectController", function($scope, $location, $rootScope, userFactory){

	// Titel van deze pagina
	$rootScope.pageTitle = false;

	$scope.formData = {};

	$scope.init = function() {

	};

});